{
  "name": "lemma-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Lemma backend: AI proxy with server-side metering, subscription entitlements, family seats, and consent-enforced social graph.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js && node --check lib/plans.js && node --check lib/entitlements.js && node --check lib/social.js && node --check lib/ai.js && node --check lib/play.js && node --check lib/otp.js",
    "serve": "firebase emulators:start --only functions,database"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "googleapis": "^144.0.0",
    "nodemailer": "^6.9.0"
  }
}
